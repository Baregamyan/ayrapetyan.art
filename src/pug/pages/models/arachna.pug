extends ../../common/layout

block vars
  -
    const data = {
      href: `models/${arachna.id}.html`,
      main: false,
      title: `${arachna.name} Model - Yervand Ayrapetyan`,
      description: `Yervand Ayrapetyan's ${arachna.title} model`,
      keywords: `${arachna.name}, 3D Model, Yervand Ayrapetyan`,
      url: `http://ayrapetyan.art/models/${arachna.id}.html`,
      image: "og",
      name: "Yervand Ayrapetyan's web-site"
    }

block content
  .overlay
  +model(arachna)
