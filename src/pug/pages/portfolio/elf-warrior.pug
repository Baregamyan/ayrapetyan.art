extends ../../common/layout

block vars
  -
    const data = {
      href: `portfolio/${elfWarrior.id}.html`,
      main: false,
      title: `${elfWarrior.name} Model - Yervand Ayrapetyan`,
      description: `Yervand Ayrapetyan's ${elfWarrior.name} model`,
      keywords: `${elfWarrior.name}, 3D Model, Yervand Ayrapetyan`,
      url: `http://ayrapetyan.art/portfolio/${elfWarrior.id}.html`,
      image: "og",
      name: "Yervand Ayrapetyan's web-site"
    }

block content
  .overlay
  +model(elfWarrior)
