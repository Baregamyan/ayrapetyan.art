extends ../../../common/layout

block vars
  -
    const data = {
      href: `portfolio/concept-art/${longHandlesVariants.id}.html`,
      main: false,
      title: `${longHandlesVariants.name} Model - Yervand Ayrapetyan`,
      description: `Yervand Ayrapetyan's ${longHandlesVariants.name} model`,
      keywords: `${longHandlesVariants.name}, 3D Model, Yervand Ayrapetyan`,
      url: `http://ayrapetyan.art/portfolio/concept-art/${longHandlesVariants.id}.html`,
      image: "og",
      name: "Yervand Ayrapetyan's web-site"
    }

block content
  .overlay
  +model(longHandlesVariants)
