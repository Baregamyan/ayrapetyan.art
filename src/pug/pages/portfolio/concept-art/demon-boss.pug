extends ../../../common/layout

block vars
  -
    const data = {
      href: `portfolio/concept-art/${bowVariants.id}.html`,
      main: false,
      title: `${bowVariants.name} Model - Yervand Ayrapetyan`,
      description: `Yervand Ayrapetyan's ${bowVariants.name} model`,
      keywords: `${bowVariants.name}, 3D Model, Yervand Ayrapetyan`,
      url: `http://ayrapetyan.art/portfolio/concept-art/${bowVariants.id}.html`,
      image: "og",
      name: "Yervand Ayrapetyan's web-site"
    }

block content
  +model(demonBoss)
